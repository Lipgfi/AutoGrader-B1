# 第十二周周报

## 一、已完成工作

### 1. 前端类型定义修复

**问题描述**：前端 API 类型与后端 SQL 数据库字段命名不一致（camelCase vs snake_case）

**修改文件**：`src/types/api.ts`

**修改内容**：
- `UserInfo` 类型：添加 `user_id`、`real_name`、`student_id`、`teacher_id`、`avatar_url`、`is_active`、`created_at`、`last_login_at` 字段
- `Course` 类型：添加 `course_id`、`course_code`、`course_name`、`teacher_id` 字段
- `Class` 类型：添加 `class_id`、`course_id`、`class_name`、`class_code`、`teacher_id` 字段
- `Assignment` 类型：添加 `assignment_id`、`class_id`、`teacher_id`、`due_date`、`is_published`、`allow_resubmit`、`published_at`、`question_id` 字段
- `Question` 类型：添加 `question_id`、`description`、`type`、`difficulty`、`language`、`time_limit`、`memory_limit`、`starter_code`、`solution_code`、`is_active` 字段
- `Submission` 类型：添加 `submission_id`、`student_user_id`、`question_id`、`assignment_id`、`submitted_at`、`overall_score`、`passed_count`、`total_count`、`overall_comment`、`static_issues`、`case_results`、`teacher_score_override`、`override_reason` 字段

### 2. 用户状态管理更新

**修改文件**：`src/stores/user.ts`

**修改内容**：
- 更新 `userId` 计算属性，优先使用 `user_id` 字段
- 更新 `realName` 计算属性，支持 `real_name`、`realName`、`name` 三种命名方式
- 新增 `studentId`、`teacherId`、`department`、`email` 计算属性

### 3. 语言下拉框添加 Shell 选项

**问题描述**：学生作业页面语言下拉框缺失 "Shell" 选项

**修改文件**：
- `src/views/student/CodingPage.vue`
- `src/views/StudentSubmit.vue`
- `src/components/CodeEditor/index.vue`
- `src/views/QuestionBank.vue`

**修改内容**：在所有语言选择下拉框中添加 Shell 选项

### 4. 学生个人信息页面扩展

**问题描述**：学生登录后个人信息展示不完整

**修改文件**：`src/views/student/Profile.vue`

**新增展示字段**：
| 字段 | 说明 |
|------|------|
| 密码状态 | 显示是否已修改初始密码 |
| 用户ID | 用户唯一标识 |
| 创建时间 | 账户创建时间 |
| 最后登录 | 最后登录时间 |

### 5. Package.json 更新

**修改文件**：`package.json`

**修改内容**：
- 添加测试依赖：`vitest`、`@vue/test-utils`
- 添加脚本命令：
  - `test`: 启动测试（watch 模式）
  - `test:run`: 运行测试并退出
  - `typecheck`: 类型检查

### 6. Mock 数据更新

**修改文件**：`src/mock/user.ts`

**修改内容**：更新学生模拟数据，添加完整字段支持

### 7. 登录界面权限问题修复

**问题描述**：登录时学生端登录教师账号会进入教师界面，教师端登录学生账号会进入学生界面

**修改文件**：`src/views/Login.vue`、`src/router/index.ts`、`src/stores/user.ts`

**修改内容**：
- 在登录接口返回后验证用户角色与选择的角色是否匹配
- 添加角色不匹配时的错误提示
- 路由守卫增加角色校验逻辑

### 8. 作业发布功能 Bug 修复

**问题描述**：作业无法发布

**接口规范**：

| 方法 | 接口 | 说明 |
|------|------|------|
| GET | `/api/v1/assignments` | 获取作业列表 |
| POST | `/api/v1/assignments` | 创建作业 |
| GET | `/api/v1/assignments/{id}` | 获取作业详情 |
| PUT | `/api/v1/assignments/{id}` | 修改作业 |
| POST | `/api/v1/assignments/{id}/publish` | 发布作业 |

**修改文件**：`src/api/assignment.ts`、`src/views/teacher/AssignmentManagement.vue`

**修改内容**：
- 修复 `POST /api/v1/assignments/{id}/publish` 接口调用
- 调整请求参数格式符合后端要求
- 添加接口错误处理和提示信息

### 9. UI 界面优化

**需求描述**：优化 UI 界面展示效果，颜色不要过于单一

**修改文件**：`src/style/variables.scss`、`src/style/index.scss`

**修改内容**：
- 增加主题色彩变化，添加多种颜色变量
- 添加渐变色效果
- 优化卡片阴影和边框样式
- 增加交互反馈动画

### 10. 班级管理功能修复与界面分离

**问题描述**：教师端班级管理出现问题，无法新建班级

**修改文件**：`src/views/teacher/CourseManagement.vue`、`src/views/teacher/ClassManagement.vue`、`src/router/index.ts`

**修改内容**：
- 分离课程管理和班级管理路由
- 创建独立的班级管理页面
- 实现班级创建、编辑、删除功能
- 确保班级与课程的关联关系正确

---

## 二、待修复问题

暂无

---

## 三、下周工作计划

1. 完成功能测试验证
2. 优化系统性能
3. 修复已知 Bug
4. 代码审查与重构
5. 准备版本发布

---

**报告日期**：2026年6月1日  
**报告人**：开发团队